# Poster

The submitted poster goes here as `poster.pdf`. The figures it should reference
are produced by `notebooks/05_generate_results_figures_colab.ipynb` and saved
to `results/figures/`.

Required figures (PLAN.md §7 — notebook 05):

1. `main_results_bar.png` — naive vs DLinear vs PatchTST.
2. `patching_efficiency.png` — token count `N` vs MSE.
3. `patch_size_sweep.png` and `lookback_sweep.png`.
4. `example_predictions.png`.
